name: Keep App Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping pokealert
        run: curl -f https://pokealert.onrender.com/ || exit 0